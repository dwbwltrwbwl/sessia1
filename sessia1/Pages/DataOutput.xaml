<Page x:Class="sessia1.Pages.DataOutput"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:sessia1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DataOutput">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                <TextBlock Text="Фильтрация по типу"/>
                <ComboBox x:Name="ComboFilter" Height="20" Width="200" Margin="10,0,0,0" SelectionChanged="ComboFilter_SelectionChanged">
                    <ComboBoxItem Content="Все типы"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>
        
        <ListView x:Name="listProducts" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width = "250"/>
                                <ColumnDefinition Width = "350"/>
                                <ColumnDefinition Width = "250"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding title}" FontWeight="Bold"/>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Grid.Column="1">
                                <TextBlock Width="auto" Height="auto">
                                    <Run Text="Тип: "/>
                                    <Run Text="{Binding prod_types.prod_type}"/>
                                </TextBlock>
                                <TextBlock Width="auto" Height="auto">
                                    <Run Text="Артикул: "/>
                                    <Run Text="{Binding article_number}"/>
                                </TextBlock>
                                <TextBlock Width="auto" Height="auto">
                                    <Run Text="Количество человек на производстве: "/>
                                    <Run Text="{Binding production_person_count}"/>
                                </TextBlock>
                                <TextBlock Width="auto" Height="auto">
                                    <Run Text="Номер производственного цеха: "/>
                                    <Run Text="{Binding production_workshop_number}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Grid.Column="2">
                                <TextBlock Width="auto" TextWrapping="Wrap" Height="auto">
                                    <Run Text="Минимальная стоимость для агента: "/>
                                    <Run Text="{Binding min_cost_for_agent}"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <Border Grid.Row="2" Padding="10" BorderThickness="0,1,0,0" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Удалить продукт" Width="180" Height="30" Click="DeleteButton_Click" HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>